import {
  Box,
  Card,
  CardActions,
  Button,
  CardContent,
  Container,
  Typography,
  CardMedia,
  Stack,
} from "@mui/material";
import lenses from "../../static/images/lenses.PNG";

import { useRedirect } from "react-admin";

export default function Dashboard() {
  const redirect = useRedirect();
  return (
    <Box display="flex" minHeight="100vh">
      <Container maxWidth="xl">
       
      </Container>
    </Box>
  );
}
